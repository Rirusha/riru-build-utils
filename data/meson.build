install_data(
    'aliases.yml',
    install_dir: get_option('prefix') / get_option('datadir') / meson.project_name()
)

install_subdir(
    'spec-templates',
    install_dir: get_option('prefix') / get_option('datadir') / meson.project_name()
)
